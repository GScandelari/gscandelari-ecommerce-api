{
  "name": "gscandelari-ecommerce-api-notifications",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Functions (2a geracao) - servico Notifications (Fase 3, RN19), sem rotas HTTP publicas - gscandelari-ecommerce-api",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "jest --runInBand",
    "test:coverage": "jest --coverage --runInBand",
    "test:emulator": "npx firebase-tools emulators:exec --only auth,firestore --project demo-gscandelari-ecommerce-api \"npm test\"",
    "test:coverage:emulator": "npx firebase-tools emulators:exec --only auth,firestore --project demo-gscandelari-ecommerce-api \"npm run test:coverage\""
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.14.15",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.5.4"
  }
}
